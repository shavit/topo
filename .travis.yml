language: node_js
node_js:
  - "11"

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
script:
  - npm build --prod

deploy:
  provider: firebase
  token:
    secure: $DEPLOYMENT_TOKEN
  project: $DEPLOYMENT_PROJECT
